# 0.6.0 RC2 Release Notes

## Release posture

`apntalk/laravel-freeswitch-esl` `0.6.0-rc2` is the truthful next release
candidate for the `0.6.x` hardening line.

The repository has moved beyond the recorded `0.6.0-rc1` surface through
release-truth alignment, additional Laravel-boundary defensive proof, and
assignment-model persistence hardening. Because those changes were not part of
the originally recorded RC1 evidence set, the current repository state should
be treated as `0.6.0-rc2`, not promoted as unchanged RC1 and not claimed as
final `0.6.0` without fresh live validation for this exact target.

This release candidate now includes, in addition to the RC1 surface:

- corrected release-truth wording for the shipped `apntalk/esl-core` `^0.2.8`
  dependency floor in `CHANGELOG.md`
- tighter build-artifact hygiene so generated live-smoke review output remains
  outside tracked release noise
- explicit documentation that bounded health snapshot persistence stays rooted
  in `pbx_nodes` plus additive `settings_json` projection, without a dedicated
  health-history table in the current `0.6.x` scope
- explicit documentation that outbound server mode is deferred beyond the
  current `1.0.0` stabilization target and is not part of the current public
  API
- explicit documentation that the shipped Laravel event layer is the wrapper
  event surface, while higher-level Laravel-native normalized domain events
  remain deferred
- an integration proof that malformed decoded event payloads are ignored at the
  Laravel event-bridge boundary and truncated raw frames fail closed without
  dispatching Laravel events
- equivalent persistence-layer protection for
  `worker_assignments.assignment_mode`
- live-smoke workflow parity with the documented private validation wrapper and
  retained artifact layout

This release candidate still does not include:

- Laravel-owned reconnect/backoff mechanics
- Laravel-owned heartbeat/session lifecycle ownership
- Laravel-owned replay execution or session recovery
- protocol parsing or low-level transport ownership
- outbound server-mode support
- `1.0.0` stability claims

## Verification evidence

Deterministic repo-owned evidence for the current RC2 candidate surface:

- `composer validate --no-check-publish`
- `composer test`
- `vendor/bin/phpstan analyse --no-progress`
- `vendor/bin/pint --test`
- `vendor/bin/phpunit tests/Contract/PrivateLiveValidationScriptTest.php`

Private-environment live validation remains required for the exact RC2 target
before promotion.

Use [0.6.x Private Live Validation Runbook](/home/grimange/apn_projects/laravel-freeswitch-esl/docs/releases/0.6.0-private-live-validation-runbook.md)
for the bounded operator-run procedure.
Use [0.6.0 RC2 Live Smoke Evidence](/home/grimange/apn_projects/laravel-freeswitch-esl/docs/releases/0.6.0-rc2-live-smoke-evidence.md)
to record the environment-safe retained-evidence summary after a fresh RC2 run.

## Live-validation status

One fresh operator-run private-environment read-only validation pass has now
been recorded for the current RC2 candidate working tree on `main`, rooted at
commit `2147a8fc9bc72dcd579945d525bc28e217391bac`.

Recorded retained evidence is in
[0.6.0 RC2 Live Smoke Evidence](/home/grimange/apn_projects/laravel-freeswitch-esl/docs/releases/0.6.0-rc2-live-smoke-evidence.md).

Promotion beyond RC2 still requires care around release identity:

- the successful live-validation run covered the exact current local RC2
  candidate state
- promotion should use an immutable committed/taggable RC2 ref that matches
  that validated state
- final `0.6.0` still requires an explicit release decision beyond the RC2
  evidence itself

## Recommended release framing

Use this release as the draft release description baseline:

> `0.6.0-rc2` is the truthful next release candidate for the `0.6.x`
> hardening line of `apntalk/laravel-freeswitch-esl`. It keeps the current
> upstream ownership boundaries intact while tightening release truth, adding
> Laravel-boundary defensive proof for malformed inbound handling, hardening
> `worker_assignments.assignment_mode` persistence, and aligning the optional
> live-smoke workflow to the documented private validation wrapper and retained
> artifact layout. A fresh private-environment read-only validation run has been
> recorded for the current RC2 candidate state; promotion should anchor that
> validated state to an immutable RC2 ref before moving beyond the candidate.

## Deferred after RC2

- anchoring the validated RC2 candidate state to an immutable committed/tagged
  release ref
- final `0.6.0` promotion mechanics after that evidence exists
- `1.0.0` stabilization work
