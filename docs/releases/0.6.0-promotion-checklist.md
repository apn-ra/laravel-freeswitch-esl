# 0.6.0 Promotion Checklist

## Current status

The final `0.6.0` release surface is prepared from the validated immutable RC2
runtime ref `v0.6.0-rc2`, pointing at commit
`8602d3fed7f12d829d12538631b35503ac2410ba`, plus later docs-only publication
material.

Technical release blockers are currently exhausted inside this repository.

Supporting materials:

- [0.6.0 Release Notes](/home/grimange/apn_projects/laravel-freeswitch-esl/docs/releases/0.6.0.md)
- [0.6.0 RC2 Live Smoke Evidence](/home/grimange/apn_projects/laravel-freeswitch-esl/docs/releases/0.6.0-rc2-live-smoke-evidence.md)
- [0.6.x Private Live Validation Runbook](/home/grimange/apn_projects/laravel-freeswitch-esl/docs/releases/0.6.0-private-live-validation-runbook.md)

## Pre-publish confirmation

Before publishing `0.6.0`, confirm:

- the intended release target still differs from `v0.6.0-rc2` only by
  release-facing docs/materials
- the final release notes in `docs/releases/0.6.0.md` still match the chosen
  publish commit
- `git diff --stat v0.6.0-rc2..<FINAL_COMMIT>` remains limited to release docs
- `CHANGELOG.md` is promoted to the final release entry during the actual
  release action, not earlier
- the chosen remote receives the intended immutable tag/release action from the
  validated content

## Evidence summary

Deterministic repo-owned evidence:

- `composer validate --no-check-publish`
- `composer test`
- `vendor/bin/phpstan analyse --no-progress`
- `vendor/bin/pint --test`
- `vendor/bin/phpunit tests/Contract/PrivateLiveValidationScriptTest.php`

Operator-run live validation:

- `auth`: pass
- `api status`: pass
- `event-plain`: pass
- `event-json`: pass
- retained summary files present: `summary.json`, `release-review.md`

## Remaining non-repo actions

The remaining actions are maintainer-controlled release mechanics only:

- tag the chosen reviewed final publication commit as `v0.6.0`
- push the validated commit history and intended final tag to the remote
- publish the final release using the prepared notes
- promote `CHANGELOG.md` during that release action if desired by maintainer
