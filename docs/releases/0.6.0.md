# 0.6.0 Release Notes

## Status

This document prepares the final `0.6.0` publication surface from the validated
immutable RC2 runtime ref `v0.6.0-rc2`, pointing at commit
`8602d3fed7f12d829d12538631b35503ac2410ba`, plus later docs-only release
handoff material.

It does not claim that `0.6.0` has already been published. It records the
release-facing material that would be used for the final promotion step once a
maintainer chooses to promote the reviewed final publication commit beyond RC2.

## Release posture

`apntalk/laravel-freeswitch-esl` `0.6.0` is prepared from a release candidate
surface with:

- deterministic repo-owned gates green locally
- successful operator-run private-environment read-only validation for the
  exact immutable runtime ref
- release-facing docs aligned to the shipped package boundary and support
  posture
- no runtime-affecting file changes after the validated RC2 ref

This release surface includes:

- the documented `apntalk/esl-core` `^0.2.8` dependency floor reflected in
  release truth
- tighter build-artifact hygiene for retained live-validation output
- explicit documentation of the shipped health projection, outbound-support
  deferral, and normalized-event deferral posture
- Laravel-boundary defensive integration proof for malformed inbound handling
- persistence-layer protection for `worker_assignments.assignment_mode`
- live-validation workflow parity with the documented wrapper and retained
  artifact layout

This release still does not claim:

- Laravel-owned reconnect/backoff lifecycle ownership
- Laravel-owned replay execution or runtime session recovery
- outbound server-mode support
- `1.0.0` stability guarantees

## Verification evidence

Deterministic repo-owned evidence:

- `composer validate --no-check-publish`
- `composer test`
- `vendor/bin/phpstan analyse --no-progress`
- `vendor/bin/pint --test`
- `vendor/bin/phpunit tests/Contract/PrivateLiveValidationScriptTest.php`

Operator-run live validation:

- ref: `v0.6.0-rc2`
- commit: `8602d3fed7f12d829d12538631b35503ac2410ba`
- executed at: `2026-04-18T07:41:41+00:00`
- retained evidence: `build/live-smoke-review/`

Applicability to the final publication candidate:

- later release-prep commits after `v0.6.0-rc2` are docs-only
- no workflow, script, config, source, or test file changed after the
  validated RC2 runtime ref
- the private live-validation evidence therefore still applies to the runtime
  artifact being promoted into final `0.6.0`

Detailed retained-evidence summary:

- [0.6.0 RC2 Live Smoke Evidence](/home/grimange/apn_projects/laravel-freeswitch-esl/docs/releases/0.6.0-rc2-live-smoke-evidence.md)

## Final promotion framing

Use this release description as the baseline for the final maintainer-driven
promotion step:

> `0.6.0` promotes the validated `0.6.x` hardening line of
> `apntalk/laravel-freeswitch-esl` without widening package ownership. It keeps
> the current upstream ESL runtime boundaries intact while tightening release
> truth, adding Laravel-boundary defensive proof for malformed inbound handling,
> hardening `worker_assignments.assignment_mode` persistence, and aligning the
> optional live-validation workflow to the documented wrapper and retained
> artifact layout. Deterministic repo-owned gates are green and a fresh
> operator-run private-environment read-only validation run has passed for the
> exact immutable RC2 runtime ref. Later final-release preparation stays
> release-facing only, so that evidence still applies to the final publication
> candidate.
